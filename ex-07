#include <stdio.h>

int main() {
  
  float fab, cons, impost, dist;

  printf("Informe o custo de fabrica\n");
  scanf("%f",&fab);

  dist=fab*0.28;
  
  impost=fab+(fab*0.45);
  
  cons=(fab+(impost+dist));

  printf(" O custo ao consumidor de um carro novo eh de %2.f reais",cons);

 return 0;
}
